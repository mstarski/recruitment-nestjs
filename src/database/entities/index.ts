import { CatEntity } from './cat.entity';
import { ClientEntity } from './client.entity';
import { ShelterEntity } from './shelter.entity';

export const entities = [CatEntity, ClientEntity, ShelterEntity];
